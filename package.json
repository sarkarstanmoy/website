{
  "name": "pixlme-admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "aws s3 sync . s3://pixlme.com --delete && aws cloudfront create-invalidation --distribution-id E13JKP7BLOFJEI --paths '/*' "
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0"
  }
}
